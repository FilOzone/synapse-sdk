/**
 * HTTP Logger interface for logging HTTP requests and responses
 *
 * Re-exported from @filoz/synapse-core for convenience.
 * Used to log HTTP requests made to PDP servers (Curio) for debugging and monitoring.
 */
export type { HTTPLogger } from '@filoz/synapse-core/http-logger'
