{
  "name": "synapse",
  "version": "0.0.0",
  "private": true,
  "description": "JavaScript packages for Filecoin Onchain Cloud",
  "license": "Apache-2.0 OR MIT",
  "workspaces": [
    "./packages/*",
    "./examples/*",
    "./docs",
    "./apps/*"
  ],
  "scripts": {
    "fix": "biome check --no-errors-on-unmatched --files-ignore-unknown=true --fix .",
    "check:markdown": "npx markdownlint-cli2 --config .github/.markdownlint-cli2.jsonc '**/*.md' '**/*.mdx'",
    "build": "npm run build --workspace=packages --if-present",
    "lint": "npm run lint --workspace=packages --if-present",
    "test": "npm run test --workspace=packages --if-present",
    "clean": "rm -rf node_modules pnpm-lock.yaml package-lock.json packages/*/{pnpm-lock.yaml,package-lock.json,coverage,.nyc_output,dist,node_modules} examples/*/{pnpm-lock.yaml,package-lock.json,coverage,.nyc_output,dist,node_modules} docs/{pnpm-lock.yaml,package-lock.json,coverage,.nyc_output,dist,node_modules,.astro} apps/*/{pnpm-lock.yaml,package-lock.json,coverage,.nyc_output,dist,node_modules}"
  },
  "devDependencies": {
    "@biomejs/biome": "~2.2.4",
    "typescript": "5.9.3"
  },
  "simple-git-hooks": {
    "pre-commit": "npm exec biome check --no-errors-on-unmatched --files-ignore-unknown=true --staged"
  },
  "engines": {
    "node": ">=22"
  }
}
